{
  "name": "reservation-saas",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,app -c blue,green \"npm run dev -w server\" \"npm run dev -w app\"",
    "build": "npm run build -w server && npm run build -w app",
    "test": "npm run test -w server && npm run test -w app",
    "lint": "npm run lint -w server && npm run lint -w app",
    "insforge:migrate": "npx @insforge/cli db migrations up --all",
    "insforge:functions:deploy": "node scripts/deploy-functions.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
